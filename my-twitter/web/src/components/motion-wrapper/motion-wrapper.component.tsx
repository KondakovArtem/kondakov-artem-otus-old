import {motion} from 'framer-motion';
import styled from 'styled-components';

export const MotionWrapperComponent = styled(motion.div)`
  display: flex;
  flex-direction: column;
  min-height: 100%;
`;
